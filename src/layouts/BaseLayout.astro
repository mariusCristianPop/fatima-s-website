---
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import CookieBanner from '../components/CookieBanner.astro';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  ogType?: string;
  canonicalUrl?: string;
  gscVerification?: string;
}

const {
  title = 'Fatima Saleh',
  description = 'Coach, facilitator, and organizational development consultant.',
  ogImage = '/og-default.jpg',
  ogType = 'website',
  canonicalUrl,
  gscVerification = import.meta.env.PUBLIC_GSC_VERIFICATION_TAG ?? '',
} = Astro.props;

const canonical = canonicalUrl ?? Astro.url.href;
const ogImageAbsolute = ogImage.startsWith('http') ? ogImage : `https://fatimasaleh.com${ogImage}`;
---

<!doctype html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="icon" type="image/png" href="/favicon.png" />
    <link rel="canonical" href={canonical} />

    <!-- Open Graph -->
    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={canonical} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageAbsolute} />
    <meta property="og:site_name" content="Fatima Saleh" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageAbsolute} />

    <!-- Google Search Console verification -->
    {gscVerification && <meta name="google-site-verification" content={gscVerification} />}

    <!-- Extra head content injected by individual pages -->
    <slot name="head" />

    <!-- Theme: apply before paint to avoid flash -->
    <script is:inline>
      (function() {
        const stored = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        // Only apply dark if user explicitly chose it OR system is dark AND media query resolves
        if (stored === 'dark' || (!stored && prefersDark && window.matchMedia('(prefers-color-scheme: dark)').media !== 'not all')) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>

    <style is:global>
      :root {
        --color-ink: #0F0E0D;
        --color-burgundy: #6B2D3E;
        --color-gold: #C9A84C;
        --color-magenta: #A8224F;
        --color-cream: #F7F4F0;
        --color-muted: #6B6460;
      }
      .dark {
        --color-ink: #F7F4F0;
        --color-cream: #141210;
        --color-muted: #9A9590;
        --color-burgundy: #8B4D5E;
        --color-gold: #D4B86A;
        --color-magenta: #E0507A;
      }
      /* Explicit resolved colours so Lighthouse contrast analyser can read them */
      .text-ink  { color: #0F0E0D; }
      .dark .text-ink  { color: #F7F4F0; }
      .text-cream { color: #F7F4F0; }
      .dark .text-cream { color: #F7F4F0; }
      .text-magenta { color: #A8224F; }
      .dark .text-magenta { color: #E0507A; }
      .text-gold { color: #C9A84C; }
      .dark .text-gold { color: #D4B86A; }
      .text-muted { color: #6B6460; }
      .dark .text-muted { color: #9A9590; }
      .bg-ink { background-color: #0F0E0D; }
      .dark .bg-ink { background-color: #0F0E0D; }
      .bg-cream { background-color: #F7F4F0; }
      .dark .bg-cream { background-color: #141210; }
      .bg-burgundy { background-color: #6B2D3E; }
      .dark .bg-burgundy { background-color: #8B4D5E; }
      .bg-white { background-color: #ffffff; }
    </style>
  </head>
  <body class="bg-cream text-ink min-h-screen flex flex-col font-sans antialiased" style="color:#0F0E0D;background-color:#F7F4F0">
    <Nav />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
    <CookieBanner />
  </body>
</html>
